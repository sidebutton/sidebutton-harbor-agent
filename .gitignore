# Python build / packaging
build/
dist/
*.egg-info/
src/*.egg-info/
__pycache__/
*.py[cod]

# Virtualenvs
.venv/
venv/

# Test / tooling caches
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# Smoke-run artifacts (see "Smoke run" in the README): harbor writes trials under
# ./jobs and downloads the dataset into ./<dataset-name>. Both are large and
# neither belongs in the repo.
jobs/
terminal-bench-2-1/

# local dev venv (agent workspace)
.venv-sb/
